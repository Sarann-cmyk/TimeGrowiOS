//
//  SyncConfiguration.swift
//  TimeGrow
//

import Foundation

enum SyncConfiguration {
    // Shared across every device you want to sync together (Mac + iPhone).
    // Copy this exact string into the iOS app's SyncConfiguration as well.
    static let syncCode = "8EB71F23-BD83-4B66-BD45-C088B2DF5456"
}
